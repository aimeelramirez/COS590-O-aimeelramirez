// src/services/ApiService.ts
class ApiServices {
    static makeApiCall(url: string, payload: any) {
      return new Promise((resolve, reject) => {
        // API call logic here
        // Resolve or reject based on API response
      });
    }
  
    // Add other API service methods as needed
  }
  
  export default ApiServices;